function flatten(arr){
    var arr2=[]
     function solve(arr){
       for(var i=0;i<arr.length;i++){
         if(typeof(arr[i])===Arry){
              argument.callee(arr[i])
        }else{
             arr2.push(arr[i])
       }
    }
  }
}